## Summary

What does this PR change and why?

## Checklist

- [ ] `ruff check` and `ruff format --check` pass
- [ ] `mypy LLmThoughtLens` passes
- [ ] `pytest` passes (new tests assert real semantics, not just "not None")
- [ ] No synthesized internals — `evidence_kind` stays honest
- [ ] Docs / CHANGELOG updated if behavior changed

## Verification

How did you verify this end-to-end (commands, providers, sample output)?
